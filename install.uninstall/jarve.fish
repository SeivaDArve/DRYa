#!/usr/bin/fish
# Title: Installing jarve from a fish shell

alias jv="~/.jarve/jv.sh"
funcsave jv

# aasking fish shell to execute a script in a diferent language, bash in this case

bash
./jarve.bash
exit
