# Title: README (directory: 'var')

# List of files in this directory:
   - 'once-tasks-list.txt' : It is used to store a list of things to update in all other DRYa machines. When there is a major update that all other machines must take automatically, that action (of set of functions) will be coded here. The file might contain code that only runs is some specific foreign machine (example: dryaSRC renamed at ~/.bashrc and such update may not self apply. If it cannot be applied, it will be mentioned in this ToDo list
