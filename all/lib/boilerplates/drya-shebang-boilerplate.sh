#/bin/bash
# Title:
# Description: 
# Use:

# Usefull variables:
   __name__="file.sh"                  # Used to descride the name of current file with extention. Example: .exe .jpg .mp3
   __repo__="${v_REPOS_CENTER}/Files"  # Used to descride the name of current repo, the repository that contains __name__
   v_ftf_talk="File: "                 # Used to better present text at `fzf` menus in the prompt area
