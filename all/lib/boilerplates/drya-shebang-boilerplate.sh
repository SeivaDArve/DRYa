#/bin/bash
# Title: example.sh
# Description: 
# Use:

# Usefull variables:
   __name__="example.sh"                     # Used to describe the name of current file with extention. Example: .exe .jpg .mp3
   __repo__="${v_REPOS_CENTER}/RepoExample"  # Used to describe the name of current repo, the repository that contains __name__
   v_ftf_talk="example.sh: "                 # Used to better present text at `fzf` menus in the prompt area
