# Title: nan-D

This script should search the entire file system and locate files that contain instructions of changes to be applied

example: 
   write "cp ./ $my_backups" on a file called .D in at some directory on your file system. And when nan-D locates it, applies what is written in it
