# Title: Directory 'termux-widget'
# Use: 1. copy ./.shortcuts to ~/.shortcuts
#      2. Install 'termux:widget' for example from the app store 'f-droid'
#      3. Long tap the smatphone main screen to either drop a script (from .shortcuts) with or without a custom icon (from .shortcuts/icons). Or, it can also be droped a window with an acess list of all the icons in .shortcuts

# Content:
   directory: .shortcuts/        # Contains all scripts that termux:widget can place on smartphone's mains screen 
   directory: .shortcuts/icons/  # Contains all the icons for the scripts. The icons require the same names to be used
